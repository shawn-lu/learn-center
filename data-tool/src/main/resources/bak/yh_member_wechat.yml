dataSourceKey: defaultDS
destination: yh-member-wechat-binlog
esMapping:
  _index: member_yh_member_wechat
  _id: _id
  sql: select a.id as _id,a.member_id as memberId,a.app_id as appId,a.open_id as openId,a.created_at
    as createdAt,a.created_by as createdBy,a.last_updated_at as lastUpdatedAt,a.id
    as _id,a.member_id as memberId,a.app_id as appId,a.open_id as openId,a.created_at
    as createdAt,a.created_by as createdBy,a.last_updated_at as lastUpdatedAt from
    yh_member_wechat a
  commitBatch: 3000
groupId: group_yh-member-wechat
outerAdapterKey: yh_member_wechat-es7-key
