dataSourceKey: defaultDS
destination: t-member-mobile-binlog
esMapping:
  _index: member_t_member_mobile
  _id: _id
  sql: select a.id as _id,a.platform as platform,a.member_id as memberId,a.member_no
    as memberNo,a.mobile as mobile,a.created_at as createdAt,a.created_by as createdBy,a.last_updated_at
    as lastUpdatedAt,a.agent_support_tag as agentSupportTag,a.id as _id,a.platform
    as platform,a.member_id as memberId,a.member_no as memberNo,a.mobile as mobile,a.created_at
    as createdAt,a.created_by as createdBy,a.last_updated_at as lastUpdatedAt,a.agent_support_tag
    as agentSupportTag from t_member_mobile a
  commitBatch: 3000
groupId: group_t-member-mobile
outerAdapterKey: t_member_mobile-es7-key
