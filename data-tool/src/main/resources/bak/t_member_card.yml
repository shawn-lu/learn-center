dataSourceKey: defaultDS
destination: t-member-card-binlog
esMapping:
  _index: member_t_member_card
  _id: _id
  sql: select a.id as _id,a.platform as platform,a.member_id as memberId,a.member_no
    as memberNo,a.card_no as cardNo,a.card_type as cardType,a.card_status as cardStatus,a.shop_id
    as shopId,a.issue_date as issueDate,a.expired_date as expiredDate,a.created_at
    as createdAt,a.created_by as createdBy,a.updated_at as updatedAt,a.updated_by
    as updatedBy,a.last_updated_at as lastUpdatedAt,a.agent_support_tag as agentSupportTag,a.id
    as _id,a.platform as platform,a.member_id as memberId,a.member_no as memberNo,a.card_no
    as cardNo,a.card_type as cardType,a.card_status as cardStatus,a.shop_id as shopId,a.issue_date
    as issueDate,a.expired_date as expiredDate,a.created_at as createdAt,a.created_by
    as createdBy,a.updated_at as updatedAt,a.updated_by as updatedBy,a.last_updated_at
    as lastUpdatedAt,a.agent_support_tag as agentSupportTag from t_member_card a
  commitBatch: 3000
groupId: group_t-member-card
outerAdapterKey: t_member_card-es7-key
