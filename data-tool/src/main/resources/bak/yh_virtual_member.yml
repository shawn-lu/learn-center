dataSourceKey: defaultDS
destination: yh-virtual-member-binlog
esMapping:
  _index: member_yh_virtual_member
  _id: _id
  sql: select a.id as _id,a.member_id as memberId,a.shop_id as shopId,a.created_at
    as createdAt,a.created_by as createdBy,a.last_updated_at as lastUpdatedAt,a.updated_by
    as updatedBy,a.id as _id,a.member_id as memberId,a.shop_id as shopId,a.created_at
    as createdAt,a.created_by as createdBy,a.last_updated_at as lastUpdatedAt,a.updated_by
    as updatedBy from yh_virtual_member a
  commitBatch: 3000
groupId: group_yh-virtual-member
outerAdapterKey: yh_virtual_member-es7-key
