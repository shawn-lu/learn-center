dataSourceKey: defaultDS
destination: yh-unionid-member-binlog
groupId: group_yh-unionid-member
outerAdapterKey: yh_unionid_member-es7-key
esMapping:
  _index: member_yh_unionid_member
  _id: _id
  sql: select a.id as _id,a.id as id,a.union_id as unionId,a.member_id as memberId,a.created_at
    as createdAt,a.created_by as createdBy,a.updated_at as updatedAt,a.updated_by
    as updatedBy,a.last_updated_at as lastUpdatedAt,a.id as _id,a.id as id,a.union_id
    as unionId,a.member_id as memberId,a.created_at as createdAt,a.created_by as createdBy,a.updated_at
    as updatedAt,a.updated_by as updatedBy,a.last_updated_at as lastUpdatedAt from
    yh_unionid_member a
  commitBatch: 3000
